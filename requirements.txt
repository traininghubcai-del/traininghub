# M&A Supply Training Hub — runtime dependencies.
#
#   python3 -m venv .venv
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/python server.py
#
# The app itself is Python stdlib (http.server + sqlite3). These four are the
# only third-party pieces:

openpyxl==3.1.5     # reads/writes the class catalog + registrations xlsx mirror
segno==1.6.6        # QR codes on the printable class sheets (src/qr_pack.py)
reportlab==5.0.0    # per-student reminder letters as PDF (src/reminder_letters.py)
PyYAML==6.0.3       # trainer / TM code files under data/ (src/admin_hub.py)
pypdf==6.14.2       # reads generated PDFs back in tests / audits
